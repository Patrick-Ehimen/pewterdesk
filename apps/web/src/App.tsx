// v2, deferred. No SecretStore implementation here, by design — and most
// exchanges will need a thin CORS proxy in front of this build.
export function App() {
  return (
    <main>
      <h1>PewterDesk (web)</h1>
    </main>
  );
}
